#include <iostream>
#include <cmath>
using namespace std;

int main() {
    double x, y;
    cin >> x >> y;

    if (y > abs(x))
        cout << "Да";
    else if (y == abs(x))
        cout << "На границе";
    else
        cout << "Нет";

    return 0;
}
